aiofiles==25.1.0
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aioice==0.10.2
aiortc==1.15.0
aiosignal==1.4.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
attrs==26.1.0
av==17.1.0
bcrypt==4.0.1
beanie==1.26.0
boolean.py==5.0
CacheControl==0.14.4
cattrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click>=8.4.2,<9.0.0
colorama==0.4.6
coremltools==9.0
cryptography==50.0.0
ctranslate2==4.8.1
cyclonedx-python-lib==11.12.0
deepgram-sdk==7.7.0
defusedxml==0.7.1
Deprecated==1.3.1
distro==1.9.0
dnspython==2.8.0
docopt==0.6.2
docstring_parser==0.18.0
ecdsa==0.19.2
email-validator==2.3.0
fakeredis==2.37.1
fastapi==0.141.1
faster-whisper==1.2.1
filelock==3.32.4
flatbuffers==25.12.19
frozenlist==1.8.0
fsspec==2026.7.0
google-crc32c==1.8.0
h11==0.16.0
h2==4.4.1
hf-xet==1.6.0
hpack==4.2.0
httpcore==1.0.9
httpcore2==2.10.0
httptools==0.8.0
httpx==0.28.1
httpx2==2.10.0
huggingface_hub==1.29.0
hyperframe==6.1.0
idna==3.18
ifaddr==0.2.0
iniconfig==2.3.0
Jinja2==3.1.6
jiter==0.16.0
joblib==1.5.3
lazy-model==0.2.0
license-expression==30.4.4
limits==5.8.0
llvmlite==0.49.0
loguru==0.7.3
Markdown==3.10.3
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
motor==3.5.0
mpmath==1.3.0
msgpack==1.2.2
msgspec==0.21.1
multidict==6.7.1
networkx==3.6.1
nltk==3.10.3
num2words==0.5.14
numba==0.67.0
numpy==2.4.6
onnxruntime==1.24.4
openai==2.54.0
opencv-python-headless==4.14.0.94
orjson==3.12.0
packageurl-python==0.17.6
packaging==26.3
passlib==1.7.4
pathvalidate==3.3.1
pillow==12.3.0
pinecone==9.1.0
# Task 4.3/4.7 — reads real memory/CPU figures from the OS. Used to decide
# whether the worker pool may grow (4.3) and as one of the "actually verify
# a dependency" checks behind /health (4.7), not just a static OK.
psutil==7.2.2
pip-api==0.0.34
pip-requirements-parser==32.0.1
pip_audit==2.10.1
pipecat-ai==1.7.0
piper-tts==1.7.0
platformdirs==4.11.5
pluggy==1.6.0
propcache==0.5.2
protobuf==6.33.6
py-serializable==2.1.0
pyaml==26.7.0
pyasn1==0.6.4
pycparser==3.0
# Task 4.9 — the /metrics scrape endpoint. Just an exporter library; nothing
# is scraped until an operator points a Prometheus instance at this server,
# which is why turning this dependency on carries zero cost or behaviour
# change by itself.
prometheus_client==0.26.0
pydantic==2.13.4
pydantic-settings==2.15.0
pydantic_core==2.46.4
pyee==13.0.1
Pygments==2.21.0
pylibsrtp==1.0.0
pyloudnorm==0.2.0
pymongo==4.8.0
pyOpenSSL==26.4.0
pyparsing==3.3.2
pypdf==6.16.2
pytest==9.1.1
pytest-asyncio==1.4.0
python-dotenv==1.2.2
python-jose==3.5.0
python-multipart==0.0.32
PyYAML==6.0.3
regex==2026.7.19
# Task 4.1/4.2/4.5 — shared state across API replicas (the active-call
# registry, the call queue, the concurrency counter). Inert unless
# settings.redis_url is set; a blank REDIS_URL (the default) keeps every one
# of those in the single process's own memory, exactly as before Phase 4.
redis==8.1.0
requests==2.34.2
resampy==0.4.3
rich==15.0.0
rsa==4.9.1
ruff==0.16.5
safetensors==0.8.0
scipy==1.17.1
sentry-sdk==2.68.1
shellingham==1.5.4
six==1.17.0
slowapi==0.1.10
sniffio==1.3.1
sortedcontainers==2.4.0
soxr==1.0.0
starlette==1.6.0
sympy==1.14.0
tokenizers==0.23.1
toml==0.10.2
tomli==2.4.1
tomli_w==1.2.0
# torch/torchaudio deliberately NOT pinned here. They were captured by a
# `pip freeze` on the Windows dev machine, but nothing actually needs them:
# `Required-by` is empty, faster-whisper runs on CTranslate2, and the VAD in
# use is SileroVADAnalyzer, which is onnxruntime. Every package that mentions
# torch does so behind an optional extra (pipecat-ai[local-smart-turn],
# transformers[torch], piper-tts[train]) that this project does not install.
#
# It mattered because on Linux pip resolves torch to the CUDA build: roughly
# 10GB of GPU libraries, downloaded into a container running on a 4GB
# e2-medium that has no GPU and never will. That nearly filled the 30GB disk
# on a routine rebuild.
#
# If local Smart Turn detection is ever adopted, add torch back explicitly
# AND pin the CPU wheels:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
tqdm==4.70.0
transformers==5.16.1
truststore==0.10.4
typer==0.27.2
typing-inspection==0.4.4
typing_extensions==4.16.0
# Task 3.5 — the IANA time zone database as a package. Linux has one in the
# OS, Windows does not, so zoneinfo.ZoneInfo("Asia/Kolkata") raises there
# and the booking template would fall back to UTC on a developer machine
# while working on the server. Pinning it makes both behave identically.
tzdata==2026.1
urllib3==2.7.0
uvicorn==0.52.3
wait_for2==0.4.1
watchfiles==1.2.0
websockets==17.0.1
win32_setctime==1.2.0
wrapt==2.4.0
yarl==1.24.5

# Task 2.7 — OpenTelemetry export of pipecat's per-stage call traces to
# Langfuse. Plain OTLP, so this is not Langfuse-specific: any OTel backend
# works by changing the endpoint. Inert unless the langfuse_* settings are
# filled in (see app/core/tracing.py).
opentelemetry-sdk>=1.30.0,<2.0.0
opentelemetry-exporter-otlp-proto-http>=1.30.0,<2.0.0
